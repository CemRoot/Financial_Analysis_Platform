# Django settings
SECRET_KEY=your_django_secret_key_here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database settings
DB_NAME=FinancialMarketDB
DB_USER=postgres
DB_PASSWORD=your_db_password_here
DB_HOST=localhost
DB_PORT=5432

# API Keys
NEWS_API_KEY=your_news_api_key_here
GOOGLE_CLOUD_PROJECT=your_google_cloud_project_here
GOOGLE_APPLICATION_CREDENTIALS=path_to_your_credentials.json

# Machine Learning Settings
ML_MODELS_DIR=apps/predictions/ml_models/saved_models
ML_TEMP_DATA_DIR=apps/predictions/ml_models/temp_data

# Data Collection Settings
STOCK_SYMBOLS=AAPL,MSFT,GOOGL,AMZN,META,TSLA,NVDA,JPM,V,WMT
DEFAULT_TIMEFRAME=1y
UPDATE_INTERVAL=3600
NEWS_DAYS_BACK=7

# JWT Settings
# Token lifetime in minutes (default: 60)
ACCESS_TOKEN_LIFETIME=60
# Refresh token lifetime in days (default: 7)
REFRESH_TOKEN_LIFETIME=7

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
CORS_ALLOW_CREDENTIALS=True
CORS_ALLOW_ALL_ORIGINS=True

# Cache Settings
CACHE_TIMEOUT=86400
MAX_ENTRIES=1000 